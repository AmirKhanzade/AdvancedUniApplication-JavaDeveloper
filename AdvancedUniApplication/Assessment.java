public interface Assessment {
    //You will now add the abstract methods to be implemented by the Learner class.
    // The assignmentScore() method is needed to compute the marks scored by the student in assignments.
    // The quizScore() method is implemented to compute the marks scored in the final quiz.

    public void assignmentScore(int marks);
    public void quizScore(int marks);


}
